/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['images.unsplash.com', 'res.cloudinary.com','twitter.com',"images.pexels.com"]
  }
};

export default nextConfig;